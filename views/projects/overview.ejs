
<% include ../partials/headers %>


<div class="d-flex" id="wrapper" style="margin-top: 0.9%">
    <% include ../partials/sidebar %>
        <div class="container-fluid">
            <div class="container-fluid">
                <h2 style="margin-top: 1%">Overview</h2>
                <hr class="mt-1">
            </div>
            <div class="container-fluid">
                <h3 style="margin-top: 1%"><%=overview.name %></h3>
                <div class="card">
                    <div class="card-body">
                        <div class="container">
                            <div class="row">
                                <div class="col-12 col-md-8"> Issues Tracker
                                    <ul>
                                        <%data.forEach(item => { %>
                                            <li><%=item.tracker%>  : <%= count.filter(ex => ex.tracker == item.tracker)[0].opens %> Open / <%=item.total %> </li>
                                        <% });%>
                                    </ul>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="card mt-5">
                    <div class="card-body">
                        <div class="container">
                            <div class="row">
                                <div class="col-12 col-md-8"> Members
                                    <ul>
                                        <% users.forEach(user => { %>
                                            <li><%= user.firstname + ''+ user.lastname %></li>
                                        <% }); %>
                                    </ul>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

</body>

</html>